# OS Shell Playground

Small project to setup a CentOS 7 machine
via Vagrant to play with [OSQuery](https://osquery.io/).

## Usage

````
$ cd vagrant
$ vagrant up
$ vagrant ssh
$ cd /queries
$ ls -l
````
